# VoiceDock Xcode Project

This project uses Swift Package Manager as its primary build system.

To open in Xcode:
```bash
open Package.swift
```

Or generate an Xcode project:
```bash
swift package generate-xcodeproj
```

The SPM build is the canonical build method for VoiceDock.